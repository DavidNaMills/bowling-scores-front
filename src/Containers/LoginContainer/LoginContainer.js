import React from 'react';

const LoginContainer = () => {

    return (
        <div>
            display a title
            create a form using hook
        </div>
    )
};

export default LoginContainer;